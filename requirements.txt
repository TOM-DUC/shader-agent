# === LLM 客户端 ===
openai>=1.40.0          # DeepSeek 兼容 OpenAI SDK
httpx>=0.27.0
tenacity>=8.2.0         # 重试

# === 配置与环境 ===
python-dotenv>=1.0.1
pyyaml>=6.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0

# === 日志与开发体验 ===
loguru>=0.7.2
rich>=13.7.0

# === 异步与并发 ===
anyio>=4.4.0

# === 网络与抓取 ===
requests>=2.32.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0

# === 阶段二新增：知识库 ===
# 向量库（本地持久化）
chromadb>=0.5.5
# 本地嵌入模型加载
sentence-transformers>=3.0.1
huggingface-hub>=0.24.0
# 数值与表格
numpy>=1.26.0
pandas>=2.2.0

# === 阶段六新增：headless GLSL 渲染 ===
# 轻量 OpenGL 包装，pip 一行装好，跨平台
moderngl>=5.10
# 截图 PNG 编码 + 给多模态 LLM 用
Pillow>=10.0
# Windows 下需 glcontext 提供 standalone context；其它平台 moderngl 自带
glcontext>=2.5; sys_platform == "win32"

# === 测试 ===
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-rerunfailures>=14.0   # 给 v4-pro 偶发抖动的 chat_smoke 用 --reruns

# === 阶段七新增：Gradio UI ===
gradio>=4.40,<5.0          # Blocks API + gr.Code(language=...) + gr.Examples
