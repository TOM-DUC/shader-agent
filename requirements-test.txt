# === 接口自动化测试 ===
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-rerunfailures>=14.0     # 对已知抖动用例做有限重试，并在报告里标注
pytest-xdist>=3.6.0            # 并行执行：-n auto
pytest-timeout>=2.3.0          # 单用例超时熔断，避免挂死拖垮流水线
httpx>=0.27.0                  # TestClient 依赖 + 跨进程模式的 HTTP 客户端
jsonschema>=4.22.0             # 响应契约校验
allure-pytest>=2.13.0          # 测试报告
coverage[toml]>=7.5.0
pytest-cov>=5.0.0

# === 性能测试 ===
locust>=2.29.0
